# Collections

Collections are saved views over targets.

## Rules

- Prefer selector-based membership.
- Allow optional static target membership.
- Keep selectors simple for the MVP.
- Collections should reflect labels, not duplicate discovery state.

## Examples

- `production` -> `env=prod`
- `lab` -> `env=lab`
- `platform` -> `team=platform`

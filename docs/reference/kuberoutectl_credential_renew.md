---
title: "kuberoutectl credential renew"
parent: Command reference
layout: default
description: "Renew or re-authenticate a credential (if the provider supports it)"
---

## kuberoutectl credential renew

Renew or re-authenticate a credential (if the provider supports it)

```
kuberoutectl credential renew <id> [flags]
```

### Options

```
  -h, --help   help for renew
```

### Options inherited from parent commands

```
  -o, --output string   output format: text|json (default "text")
  -v, --verbose         trace external CLI commands, exit codes, and their stderr on failure
```

### SEE ALSO

* [kuberoutectl credential](kuberoutectl_credential.md)	 - Inspect and renew credentials


---
title: "kuberoutectl credential list"
parent: Command reference
layout: default
description: "List credentials and their health"
---

## kuberoutectl credential list

List credentials and their health

```
kuberoutectl credential list [flags]
```

### Options

```
  -h, --help              help for list
  -p, --provider string   filter by provider (aws|azure|gcp|kubeconfig)
```

### Options inherited from parent commands

```
  -o, --output string   output format: text|json (default "text")
  -v, --verbose         trace external CLI commands, exit codes, and their stderr on failure
```

### SEE ALSO

* [kuberoutectl credential](kuberoutectl_credential.md)	 - Inspect and renew credentials


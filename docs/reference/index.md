---
title: Command reference
layout: default
nav_order: 5
has_children: true
description: "Full kuberoutectl command reference — every command and flag, generated from the CLI so it never drifts."
---

# Command reference

Every `kuberoutectl` command and flag, generated directly from the CLI — so it
never drifts from the tool you're running. Pick a command from the sidebar, or
start at [`kuberoutectl`](kuberoutectl.md) for the top-level command.

Prefer the terminal? `kuberoutectl <command> --help` prints the same information.
